spring:
  datasource:
    url: jdbc:mysql://localhost:3306/apirest
    username: felinkdb
    password: Fe21Fe06h90.
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  server:
    port: 8080

swagger:
  enabled: true
  api-docs:
    path: /v2/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    root: INFO
    com.example.apirest: DEBUG